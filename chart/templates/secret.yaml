{{- if .Values.webhookSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "renovate-trigger.fullname" . }}
  labels:
    {{- include "renovate-trigger.labels" . | nindent 4 }}
type: Opaque
data:
  webhook-secret: {{ .Values.webhookSecret | b64enc | quote }}
{{- end }}
